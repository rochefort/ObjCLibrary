# ObjCLibrary

## Instalation

install it yourself as:

    $ git clone https://github.com/rochefort/ObjCLibrary.git
    $ cd ObjCLibrary
    $ pod install
    $ git submodule init
    $ git submodule update
